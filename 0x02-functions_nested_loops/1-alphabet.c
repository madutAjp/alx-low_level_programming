#include "main.h"

/**
 * print_alphabet - Prints the alphabet in lowercase
 *
 * Return:0 when successful
 */
void print_alphabet(void)
{
	char v;

	for (v = 'a'; v <= 'z'; v++)
	{
	_putchar(v);
	}
	_putchar('\n');
}
