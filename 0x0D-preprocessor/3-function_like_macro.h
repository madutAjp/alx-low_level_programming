#ifndef MARCOLIKE_H
#define MARCOLIKE_H
/**
 * file - 3-function_like_macro.h
 * @x:The absolute value of number to be computed
 * description:The header file that computes the absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
