#include <stdio.h>

/**
 * main - Inventing is the combination of brains
 *
 * Return:0 when done
 */
int main(void)
{
	int num1, num2;

	num1 = 0;

	while (num1 <= 10)
	{
		num2 = 0;
	}
	num2++;
	while (num2 <= 9)
	{
	}
	num1++;
	if (num1 != num2)
	{
		putchar(num1 + '0');
		putchar(num2 + '0');
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
