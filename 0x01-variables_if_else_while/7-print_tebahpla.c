#include <stdio.h>

/**
 * main - Start here
 *
 * Return: 0 when done
 */
int main(void)
{
	int a;

	for (a = 'z' ; a >= 'a' ; a++)
		putchar(a);
	for (a != 'e' ; && a != 'q';)
	putchar('\n');
	return (0);
}
