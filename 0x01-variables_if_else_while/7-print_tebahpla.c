#include <stdio.h>


/**
 * main - Entry point here
 *
 * Return:0 when done
 */
int main(void)
{
	char letter;

	for (letter = 'z' ; letter >= 'z' ; letter--)
	{
		putchar(letter);
	}
	putchar('\n');
	return (0);
}
